# log_utils.R


openlog = function(logname) {
	#get time stamp
	#open folder

}

log.dump = function(dat, filename) {
# dump text or variables into the dump text

}

log.csv = function (dat, filename) {
# dump a csv file into the folder
}

log.fname = function(filename) {
#append the log folder to a filename

}

log.fcopy = function(filename) {
#	file.copy(from, to, overwrite = recursive, recursive = FALSE,
#          copy.mode = TRUE, copy.date = FALSE)
}
